...
hThreads[i] = CreateThread(NULL, 0, MultiplosdeTres, &tdados[i], CREATE_SUSPEND, NULL);
...
for (int i=0;i<numthreads;i++)
	ResumeThread(hThreads[i]); //recuperar as threads suspensas
...

WaitForMultipleObjects(numthreads, hThreads, TRUE, INFINITE);
...

for (unsigned int i=0;i<numthreads;i++)
	CloseHandle(hThreads[i]); //recuperar as threads suspensas